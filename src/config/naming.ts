export const getStackName = (appName: string, nameFragment: string, constructType: string): string => {
    return `app-${appName}-${nameFragment}-${constructType}`;
};

export const getConstructName = (appName: string, nameFragment: string, constructType: string): string => {
    return `app-${appName}-${nameFragment}-${constructType}`;
};
